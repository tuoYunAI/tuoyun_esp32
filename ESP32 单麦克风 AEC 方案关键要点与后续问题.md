# ESP32 单麦克风 AEC 方案关键要点与后续问题

## 1. 目标

设备在播放 TTS（Speak）过程中持续采集用户语音，通过 AEC 消除喇叭回声，并将较干净的近端用户声音上传服务器，用于：

- VAD 检测；
- ASR 识别；
- 声纹验证；
- 普通语音打断判断。

本地唤醒词检测独立运行。检测到激活词时，设备立即向服务器发送 `AbortSpeaking`，不等待服务器 VAD、ASR 或声纹结果。

## 2. 单麦克风可行性

AEC 不要求两个麦克风。当前方案需要两路数字信号：

- `MIC`：唯一麦克风采集的数据，包含用户声音、环境声音和喇叭回声；
- `REF`：实际送往喇叭播放的 PCM，由软件提供，不是第二个麦克风。

```text
服务器 TTS PCM
      │
      ├──> Codec / 功放 / 喇叭
      │
      └──> REF 参考信号 ──────┐
                              ├──> AEC ─> 双讲门控 ─> Opus ─> 服务器
单 MIC ───────────> MIC 信号 ─┘

单 MIC 原始音频 ─> 本地唤醒词检测 ─> AbortSpeaking
```

必要条件：

1. 设备能够取得真正送入播放链路的 PCM；
2. MIC 与 REF 采样率、位宽和帧边界一致；
3. REF 不丢帧、不重复帧，不在连续播放中插入额外静音；
4. 播放增益稳定，喇叭和 Codec 不严重削波；
5. MIC 与喇叭的相对位置基本固定。

如果无法获取播放 PCM，单 MIC 无法使用当前参考信号式 AEC 获得可靠效果。

## 3. 当前 ESP32 实现

### 3.1 输入格式

ESP-SR AFE 使用 `MR` 输入格式：

- `M`：单 MIC；
- `R`：软件播放参考。

当前音频格式为：

```text
采样率：16000 Hz
位宽：16 bit
格式：S16LE
通道：1 MIC + 1 REF
```

### 3.2 播放参考

播放 PCM 同时送往扬声器和 AEC 参考缓冲区。

当前处理原则：

- 连续 TTS 数据包之间不重复添加参考延迟；
- 仅当播放中断超过约 `120 ms` 后，才重新插入约 `25 ms` 的播放启动补偿；
- REF 必须跟随实际播放顺序，不能跟随网络包到达顺序随意拼接。

当前设备实测 REF 到 MIC 的声学路径延迟约为 `15 ms`，且已保持稳定。

### 3.3 ESP-SR AFE 参数

```text
AEC 模式：AEC_MODE_FD_HIGH_PERF
AEC 滤波长度：4
NLP：AEC_NLP_LEVEL_NORMAL
AGC：关闭
设备 AEC 模式下 AFE VAD：关闭
采样率：16000 Hz
AFE 输入格式：MR
```

选择 `FD_HIGH_PERF` 是为了支持播放和用户说话同时发生的全双工场景。

`AEC_NLP_LEVEL_NORMAL` 是当前可用的最低非线性抑制等级，目的是在消除残余回声的同时，尽量减少对用户音色和声纹特征的破坏。

当前 AFE 输出相对输入约有 `64 ms` 固定延迟。

### 3.4 上传前双讲门控

AEC 输出在纯播放时仍可能包含少量 TTS 残留。服务器 VAD、ASR 或声纹模型可能将这些残留误判为用户，因此在上传前增加双讲门控。

门控逻辑：

1. REF 不活跃：直接放行 AEC 输出；
2. REF 活跃且判断为纯远端播放：上传静音；
3. REF 活跃且检测到近端用户：立即放行 AEC 输出；
4. 近端语音结束后保留约 `96 ms` 挂起，避免切掉词尾。

当前设备校准参数：

```text
REF 活跃 RMS：约 250
近端 MIC RMS：约 180
AEC/MIC 幅度比例：约 0.42
MIC/REF 幅度比例：约 0.32
双讲挂起：约 96 ms
```

这些参数与 MIC 灵敏度、播放音量、Codec 增益、喇叭位置和机壳结构有关，不能直接作为所有设备的固定参数。

### 3.5 唤醒词与上传链路分离

本地唤醒词检测读取原始 MIC，不经过 AEC 上传门控。

因此：

- 普通用户语音经过 AEC 后上传服务器；
- 激活词由设备本地直接检测；
- 激活词命中后立即发送 `AbortSpeaking`；
- 服务器声纹响应速度不会影响激活词打断速度。

## 4. PCM 诊断体系

设备同步记录四路 PCM：

| 文件 | 内容 |
| --- | --- |
| `MIC.pcm` | 原始 MIC，包含用户声音和喇叭回声 |
| `REF.pcm` | 实际播放参考 PCM |
| `AEC.pcm` | 上传门控前的 AFE AEC 输出 |
| `UPLOAD.pcm` | 实际送入 Opus 编码并上传服务器的音频 |

当前抓取时长为 2 秒。数据在设备空闲后以 `XX XX XX` 十六进制输出，避免实时日志打印影响音频处理。

```
for ch in MIC REF AEC UPLOAD; do
  awk -F'|' -v ch="$ch" '$1=="PCM_HEX" && $2==ch {print $4}' aec.log \
    | xxd -r -p > "${ch}.pcm"
done
```



分析重点：

1. `MIC` 与 `REF` 的延迟是否稳定；
2. `AEC` 相对 `MIC` 的处理延迟是否稳定；
3. 纯播放期间 `AEC` 是否仍有 TTS 残留；
4. 纯播放期间 `UPLOAD` 是否被门控压成静音；
5. 用户双讲期间 `UPLOAD` 是否保留完整词头和词尾；
6. AEC 是否明显改变用户的波形、能量和声纹特征。

## 5. 当前已验证结果

最近一轮数据表明：

- MIC/REF 时序已稳定，没有再次出现连续播放包之间额外增加约 `25 ms` 的问题；
- REF 到 MIC 的声学路径延迟约为 `15.25 ms`；
- AFE 输出延迟约为 `64 ms`；
- `UPLOAD.pcm` 静音比例约为 `53%`；
- 抓取片段后半段的纯播放回声被门控压成静音；
- 用户约 `0.6 秒` 的插话被保留；
- 服务器 VAD 已从跟随 TTS 持续触发，变成独立的用户插话片段。

这说明当前门控已经解决了“纯喇叭声音持续上传并触发服务器 VAD”的主要问题。

## 6. 服务端配套修复

### 6.1 PCM 快照

此前声纹验证返回 `ACCEPT` 后，服务器先执行 `roundEndReset()` 清空 `audioPackets`，随后 ASR 读取到空音频并报 `EmptyAudio`。

当前已改为：

1. VAD 结束时立即保存 PCM 快照；
2. 声纹注册使用该快照；
3. 声纹验证使用该快照；
4. ASR 使用同一份快照；
5. 声纹通过后仍可立即重置会话和停止 TTS，不再影响 ASR 音频。

### 6.2 声纹模板合成

`makeAudioEmbeddingTemplate` 当前接收 `List<VoicePrintTemplate>`：

- 直接提取已有声纹模板向量；
- 不再对原始 PCM 重复执行 embedding；
- 合成前校验模型版本一致；
- 校验预处理版本一致；
- 校验向量维度一致。

## 7. 当前主要问题

### 7.1 短音频声纹不稳定

服务器目前经常对单个较短 VAD 片段独立做声纹验证。

最近一轮数据：

- 小于 1 秒的 5 个片段，声纹平均分约 `0.205`；
- 约 1.4 秒以上的 4 个片段，声纹平均分约 `0.301`；
- 当前接受阈值为 `0.30`。

“打断”只有两个字，有效人声通常不足 1 秒，容易出现：

- `INSUFFICIENT_AUDIO`；
- `REJECT`；
- `UNCERTAIN`；
- 分数在阈值附近波动。

### 7.2 强双讲时用户声纹特征仍会受损

当 REF 音量较大时，AFE 的线性 AEC 和 NLP 会同时修改近端用户信号。部分强重叠帧中，AEC 输出能量可能仅剩原 MIC 的约 `19%–37%`。

可能影响：

- 用户辅音被削弱；
- 频谱包络发生变化；
- ASR 短词错误率升高；
- 声纹 embedding 与注册模板距离增大。

### 7.3 不同设备不能直接复用当前阈值

更换以下任一部分都可能改变回声路径：

- MIC 型号或灵敏度；
- Codec；
- 功放；
- 喇叭；
- 机壳结构；
- MIC 与喇叭距离；
- 播放音量；
- EQ、AGC 或动态增益策略。

当前固件二进制和门控参数不能不经测试直接复制到其他硬件。

### 7.4 喇叭削波和非线性失真

REF 是数字 PCM，但 MIC 收到的是经过 Codec、功放、喇叭和空气传播后的声音。如果播放链路发生削波或明显非线性失真，真实回声将不再是 REF 的简单线性变换，AEC 难以完全抵消。

### 7.5 服务器不能仅依赖一次声纹结果

历史日志中曾出现纯 TTS 残留声纹分数超过接受阈值的情况。即使设备端门控已降低风险，服务器仍不应将单个短片段的一次 `ACCEPT` 作为唯一安全依据。

## 8. 后续工作建议

### P0：服务器累积有效近端语音

不要对每个不足 1 秒的 VAD 片段立即做最终声纹判断。

建议：

- 累积约 `1.4–2 秒` 有效人声后再验证；
- 允许跨越短静音合并多个 VAD 片段；
- 设置最大累积时长和超时，避免无限等待；
- `INSUFFICIENT_AUDIO` 和 `UNCERTAIN` 时继续累积，不立即视为其他说话人；
- 声纹验证通过后，再将同一份 PCM 交给 ASR。

### P0：服务器排除已知 TTS

服务器拥有正在发送的 TTS 内容和播放时间窗口，可增加：

- 播放窗口标记；
- 上传片段与下发 TTS 的相关性检测；
- 纯远端残留拒绝；
- 声纹通过但与当前 TTS 高度相似时二次确认。

### P1：评估线性 AEC 输出

当前 ESP-SR AFE 输出包含 NLP。后续可增加线性 AEC 诊断通道，对比：

- 线性 AEC 输出；
- 当前 AFE/NLP 输出；
- ASR 准确率；
- 声纹分数；
- 纯播放残留能量。

如果线性 AEC 更能保留用户声纹，可考虑“线性 AEC + 双讲门控”的上传方案，但必须先验证纯播放残留不会重新触发服务器。

### P1：按设备保存校准参数

建议将以下参数改为板级或产品级配置：

- REF 启动补偿；
- AEC 输出延迟帧数；
- REF 活跃阈值；
- MIC 活跃阈值；
- AEC/MIC 比例；
- MIC/REF 比例；
- 双讲挂起时长；
- 最大安全播放音量。

### P2：降低播放链路非线性

- 限制数字 PCM 峰值；
- 避免功放和喇叭过载；
- 固定或记录播放增益；
- 避免 AEC REF 之前和实际播放之间存在不可见的动态处理；
- 必要时降低播放音量换取更稳定的声纹质量。

## 9. 新设备移植流程

### ESP32 或 ESP-SR 支持设备

1. 适配 Codec 与 I2S；
2. 确认能够取得真实播放 PCM；
3. 构造 `MR` 输入；
4. 统一到 16 kHz/S16LE；
5. 测量 MIC/REF 延迟；
6. 校准 REF 启动补偿；
7. 采集四路 PCM；
8. 校准双讲门控；
9. 验证本地唤醒词和 `AbortSpeaking`；
10. 验证服务器 VAD、ASR 和声纹。

### 非 ESP32 设备

整体架构仍可复用，但 ESP-SR API 需要替换为平台对应的 AEC 引擎，例如：

- WebRTC AEC3；
- SpeexDSP AEC；
- 芯片厂商 DSP/AEC；
- Android、Linux 或其他平台音频 AEC。

本地唤醒词、双讲门控、PCM 诊断、服务器声纹和 `AbortSpeaking` 逻辑仍可保留。

## 10. 验收标准

### 纯播放场景

- MIC 中可以听到明显喇叭声音；
- REF 与播放内容一致；
- AEC 输出残留明显下降；
- UPLOAD 接近静音；
- 服务器 VAD 不应跟随整段 TTS 持续触发；
- 纯 TTS 不应通过用户声纹验证。

### 双讲场景

- 用户词头和词尾完整；
- 播放过程中 ASR 能识别用户内容；
- 有效人声累计足够后，声纹分数稳定超过阈值；
- 用户停止后，UPLOAD 能重新回到静音；
- 不因门控频繁开关产生明显爆音或断裂。

### 激活词场景

- 播放过程中能够检测激活词；
- 设备立即发送 `AbortSpeaking`；
- 行为不依赖服务器声纹是否完成；
- 激活词检测不受上传门控影响。

## 11. 结论

当前方案适用于单 MIC 设备，其核心不是增加第二个 MIC，而是提供准确、连续且时序稳定的播放 PCM 参考。

现阶段已经解决：

- 连续播放参考延迟漂移；
- 纯播放残留持续上传；
- 服务器声纹通过后 PCM 被提前清空；
- 声纹模板参数变化后的合成逻辑。

下一阶段的重点是：

1. 服务器累积足够长的有效近端人声；
2. 降低强双讲时 AEC/NLP 对声纹特征的修改；
3. 对每种硬件重新校准延迟和门控参数；
4. 使用已知 TTS 信息进一步防止残留回声误接受。
